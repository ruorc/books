/**
 * Composition Root Barrel Export for the About Page Module.
 * Consolidates and exposes the public layout presentation view of the about workspace.
 * Keeps internal atomic component layout details encapsulated away from external routes.
 */

export { AboutPage } from './AboutPage';
